<div class="res res-btns">
  <% if @post.user.id == current_user.id %>
    <%= link_to '編集' ,"/posts/#{post.id}/edit", class: "res res-btn" %>
    <%= link_to '削除' ,"/posts/#{post.id}", method: :delete, class: "res res-btn" %>
  <% end %>
</div>